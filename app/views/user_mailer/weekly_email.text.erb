<h1>Weekly Chat news letter!</h1>

<p><b><%= Message.where(created_at: (Date.today - 7)..(DateTime.now)).count %></b> messages have been exchanged in the last week. </p>
<p><b><%= Message.where(created_at: (@user.messages.last.created_at)..(DateTime.now)).count %></b> messages have been exchanged in the last week.</p>
